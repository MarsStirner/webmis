<tbody>
<tr>
	<td>Дата поступления</td>
	<td>
		<%= Core.Date.format(appealExtraData.rangeAppealDateTime.start) %>
	</td>
</tr>
<tr>
	<td>Продолжительность лечения (дней) или дата выписки</td>
	<td>
		<% if (appealExtraData.rangeAppealDateTime.end) { %>
			<%= Core.Date.format(appealExtraData.rangeAppealDateTime.end) %>
		<% } else { %>
			<%= days %>
		<% } %>
	</td>
</tr>
<tr>
	<td>Отделение</td>
	<td>
		<%= appealExtraData.department.name %>
	</td>
</tr>
<!--<tr>
	<td>Палата</td>
	<td>
		N/A
	</td>
</tr>-->
<tr>
	<td>Койка</td>
	<td>
		<% if (lastMove && lastMove.get("bed")) { %>
			<%= lastMove.get("bed") %>
		<% } else { %>
			-
		<% } %>
	</td>
</tr>
<tr>
	<td>Лечащий врач</td>
	<td>
		<div class="row-fluid">
			<div class="span12" style="min-height: 0">
				<% if (appealExtraData.execPerson && appealExtraData.execPerson.doctor) { %>
					<%= appealExtraData.execPerson.doctor.name.full %>
				<% } else { %>
					<span class="Warning">Врач не назначен</span>
				<% } %>
			</div>
		</div>
		<div class="row-fluid">
			<div class="span12" style="min-height: 0; margin-top: .7em;">
				<%if (canAssign){%>
				<a href="#" class="assign-exec-person"><i class="icon-pencil"></i></a>
				<%}%>
			</div>
		</div>
	</td>
</tr>
</tbody>
