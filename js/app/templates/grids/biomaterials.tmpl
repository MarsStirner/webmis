<script id="biomaterials-grid" type="text/x-jquery-tmpl">
	<table class="Grid">
		<thead>
		<tr>
			<th>
				<input type="checkbox" name="all" id="select-all" title="Отметить все">
			</th>
			<th>
				<a class="Actions SortCol" data-field="time">Дата и время</a>
			</th>
			<th>
				<a class="Actions SortCol" data-field="fullName">ФИО</a>
			</th>
			<th>
				<a class="Actions SortCol" data-field="sex">Пол</a>
			</th>
			<th>
				<a class="Actions SortCol" data-field="birthDate">Дата рожд.</a>
			</th>
			<th>
				<a class="Actions SortCol" data-field="actionType.name">Биоматериал</a>
			</th>
			<!---->
			<th>
				<a class="Actions SortCol" data-field="actionType.status">Срочность</a>
			</th>
			<!--<th>-->
				<!--<a class="Actions SortCol" data-field="testTube">Пробирка</a>-->
			<!--</th>-->
			<th >
				Объём
			</th>
			<th>
				<a class="Actions SortCol" data-field="status">Статус</a>

			</th>
		</tr>
		</thead>
		<tbody>

		</tbody>
	</table>
</script>

<script id="biomaterials-grid-row" type="text/x-jquery-tmpl">
	<td class="status-code-${status.code}">
		{{if jobTicket.status!=2}}
		<input type="checkbox" class="id" name="id" value="${id}" data-job-ticket="${jobTicket.id}">
		{{/if}}
		<!-- ${id} -->
		<!--jobTicket.id = ${jobTicket.id}-->
	</td>

	<td class="status-code-${status.code}">
		{{formatDateTime jobTicket.date}}
	</td>

	<td class="status-code-${status.code}">
		${patient.name.raw}
	</td>
	<td class="status-code-${status.code}">
		${patient.sex}
	</td>
	<td class="status-code-${status.code}">
		{{formatDate patient.birthDate}}

	</td>
	<td class="status-code-${status.code} HasToolTip" data-tooltip-content="#tooltip_${id}">
		{{if jobTicket.status == 0}}
		<b><u>Кровь венозная (3)</u></b>
		{{else}}
		<u>Кровь венозная (3)</u>
		{{/if}}

		<div id="tooltip_${id}" class="ToolTip TopTail">
			<ul style="margin-bottom: 10px;">
				{{if assigner.name.raw}}
				<li>Назначил: ${assigner.name.raw}</li>
				{{/if}}
				{{if jobTicket.laboratory.name}}
				<li>Лаборатория: ${jobTicket.laboratory.name}</li>
				{{/if}}
				{{if jobTicket.label}}
				<li>Пометка: ${jobTicket.label}</li>
				{{/if}}
				{{if jobTicket.note}}
				<li>Примечание: ${jobTicket.note}</li>
				{{/if}}
			</ul>

				<b>Лабораторные исследования:</b>
			<ol>
				<li>Автоматический анализ крови (СВС- лейкоциты, гемоглобин, гематокрит, эритроциты, тромбоциты)</li>
				<li>Автоматический анализ крови (СВС- лейкоциты, гемоглобин, гематокрит, эритроциты, тромбоциты)</li>
				<li>Определение групповой принадлежности по системе АВО, определение фенотипа Резус фактора</li>
			</ol>



		</div>
	</td>

	<td class="status-code-${status.code}">
		{{if urgent}}<span class="Urgent">срочно</span>{{/if}}
	</td>
	<!--<td class="status-code-${status.code}">-->
		<!--${tubeType.shortName}-->
	<!--</td>-->
	<td class="status-code-${status.code}">
		{{if tubeType.volume}}
		${tubeType.volume}
		{{/if}}
	</td>


	<!--<td class="status-code-${status.code}">-->
		<!--{{if tubeType.color}}-->
		<!--<span class="color-label" style="background-color:${tubeType.color};"></span>-->
		<!--{{/if}}-->
	<!--</td>-->

	<td class="status-code-${status.code}">
		<span class="status-name">${status.name}</span>
	</td>
</script>

<script id="biomaterials-grid-row-error" type="text/x-jquery-tmpl">
	<td colspan="7">
		Ничего не найдено
	</td>
</script>

<script id="biomaterials-grid-row-default" type="text/x-jquery-tmpl">
	<h3>Введите необходимые данные в поля фильтров</h3>
</script>
