define(function (require) {
    var TreeButton = require('./TreeButton');

    var PrintButton = TreeButton.extend({
        // initialize: function () {

        // },
        // render: function () {

        // }
    });

    return PrintButton;
});
